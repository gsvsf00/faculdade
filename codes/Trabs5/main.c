#include "lib.h"

int main(){

    adicionar(20,0);
    adicionar(30,0);
    adicionar(40,2);
    adicionar(50,1);
    adicionar(60,2);
    imprimir();
    printf("\n");
    erase();
    imprimir();
    return 0;
}